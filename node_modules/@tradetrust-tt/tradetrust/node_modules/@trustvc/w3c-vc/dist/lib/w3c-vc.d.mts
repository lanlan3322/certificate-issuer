import { DocumentLoader, ContextDocument } from '@trustvc/w3c-context';
import { PrivateKeyPair } from '@trustvc/w3c-issuer';
import { RawVerifiableCredential, SignedVerifiableCredential, SigningResult, VerificationResult, DerivedResult } from './types.mjs';
import '@mattrglobal/jsonld-signatures-bbs';

declare const isRawDocument: (document: RawVerifiableCredential | unknown) => boolean;
declare const isSignedDocument: (document: SignedVerifiableCredential | unknown) => document is SignedVerifiableCredential;
declare const signCredential: (credential: RawVerifiableCredential, keyPair: PrivateKeyPair, cryptoSuite?: string, options?: {
    documentLoader?: DocumentLoader;
}) => Promise<SigningResult>;
declare const verifyCredential: (credential: SignedVerifiableCredential, options?: {
    documentLoader?: DocumentLoader;
}) => Promise<VerificationResult>;
declare const deriveCredential: (credential: SignedVerifiableCredential, revealedAttributes: ContextDocument, options?: {
    documentLoader?: DocumentLoader;
}) => Promise<DerivedResult>;

export { deriveCredential, isRawDocument, isSignedDocument, signCredential, verifyCredential };
