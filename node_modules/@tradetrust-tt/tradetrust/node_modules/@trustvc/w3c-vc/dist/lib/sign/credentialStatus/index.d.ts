import { CredentialStatus, VerifiableCredential } from '../../types.js';
import '@mattrglobal/jsonld-signatures-bbs';

declare const assertCredentialStatus: (cs: CredentialStatus, mode?: "sign" | "verify") => void;
declare const assertCredentialStatuses: <T extends VerifiableCredential>(credential: T, mode?: "sign" | "verify") => void;

export { assertCredentialStatus, assertCredentialStatuses };
