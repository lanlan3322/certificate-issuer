import { Bitstring } from './Bitstring.mjs';
import { BitstringStatusListOption } from './types.mjs';

declare class StatusList {
    bitstring: Bitstring;
    length: number;
    constructor({ length, buffer }: BitstringStatusListOption);
    setStatus(index: number, status: boolean): void;
    getStatus(index: number): boolean;
    encode(): Promise<string>;
    static decode({ encodedList }: {
        encodedList: string;
    }): Promise<StatusList>;
}

export { StatusList };
