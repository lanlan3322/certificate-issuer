import { VCBitstringCredentialSubjectType, CredentialStatusPurpose as CredentialStatusPurpose$1, CredentialStatusType as CredentialStatusType$1 } from './BitstringStatusList/types.mjs';

type VCBitstringCredentialSubject = {
    id?: string;
    type: VCBitstringCredentialSubjectType;
    statusPurpose: CredentialStatusPurpose$1;
    encodedList: string;
};
type CreateVCCredentialStatusOptions = {
    id?: string;
    credentialSubject: VCBitstringCredentialSubject;
};
type CredentialStatusType = CredentialStatusType$1;
type CredentialStatusPurpose = CredentialStatusPurpose$1;
type GeneralCredentialStatus = {
    id?: string;
    type: CredentialStatusType;
} & Record<string, any>;
type BitstringStatusListCredentialStatus = Required<GeneralCredentialStatus> & {
    statusPurpose: CredentialStatusPurpose;
    statusListIndex: string;
    statusListCredential: string;
};
type TransferableRecordsCredentialStatus = Omit<GeneralCredentialStatus, 'type'> & {
    type: 'TransferableRecords';
    tokenId: string;
    tokenNetwork: {
        chain: string;
        chainId: string | number;
    };
    tokenRegistry: string;
};
type RawCredentialStatusVC = {
    '@context': string | string[];
    type: string[];
    issuer: string | Record<string, any>;
    issuanceDate: string;
    validFrom?: string;
    validUntil?: string;
    expirationDate?: string;
    credentialSubject: Record<string, any>;
};
type SignedCredentialStatusVC = RawCredentialStatusVC & {
    proof: {
        type: string;
        created: string;
        proofPurpose: string;
        verificationMethod: string;
        proofValue: string;
    };
};

export type { BitstringStatusListCredentialStatus, CreateVCCredentialStatusOptions, CredentialStatusPurpose, CredentialStatusType, GeneralCredentialStatus, RawCredentialStatusVC, SignedCredentialStatusVC, TransferableRecordsCredentialStatus, VCBitstringCredentialSubject };
