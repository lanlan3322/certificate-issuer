"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getVerificationMethod = exports.resolve = exports.createResolver = exports.getProviderConfig = void 0;
const did_resolver_1 = require("did-resolver");
const ethr_did_resolver_1 = require("ethr-did-resolver");
const web_did_resolver_1 = require("web-did-resolver");
const node_cache_1 = __importDefault(require("node-cache"));
const config_1 = require("../config");
const utils_1 = require("../common/utils");
const getProviderConfig = () => {
    var _a, _b, _c;
    const provider = (0, utils_1.generateProvider)();
    const rpcUrl = ((_a = provider === null || provider === void 0 ? void 0 : provider.connection) === null || _a === void 0 ? void 0 : _a.url) || "";
    const networkName = ((_b = provider === null || provider === void 0 ? void 0 : provider._network) === null || _b === void 0 ? void 0 : _b.name) === "homestead" ? "mainnet" : ((_c = provider === null || provider === void 0 ? void 0 : provider._network) === null || _c === void 0 ? void 0 : _c.name) || "";
    if (!rpcUrl || !networkName) {
        return { networks: [{ name: "mainnet", rpcUrl: `https://mainnet.infura.io/v3/${config_1.INFURA_API_KEY}` }] };
    }
    return {
        networks: [{ name: networkName, rpcUrl: rpcUrl }],
    };
};
exports.getProviderConfig = getProviderConfig;
const didResolutionCache = new node_cache_1.default({ stdTTL: 5 * 60 }); // 5 min
const defaultResolver = new did_resolver_1.Resolver(Object.assign(Object.assign({}, (0, ethr_did_resolver_1.getResolver)((0, exports.getProviderConfig)())), (0, web_did_resolver_1.getResolver)()));
const createResolver = ({ ethrResolverConfig }) => {
    const resolverRegistry = Object.assign(Object.assign({}, (0, ethr_did_resolver_1.getResolver)(ethrResolverConfig !== null && ethrResolverConfig !== void 0 ? ethrResolverConfig : {})), (0, web_did_resolver_1.getResolver)());
    return ethrResolverConfig ? new did_resolver_1.Resolver(resolverRegistry) : defaultResolver;
};
exports.createResolver = createResolver;
const resolve = (didUrl, resolver) => __awaiter(void 0, void 0, void 0, function* () {
    const cachedResult = didResolutionCache.get(didUrl);
    if (cachedResult)
        return cachedResult;
    const didResolutionResult = resolver ? yield resolver.resolve(didUrl) : yield defaultResolver.resolve(didUrl);
    const did = didResolutionResult.didDocument || undefined;
    didResolutionCache.set(didUrl, did);
    return did;
});
exports.resolve = resolve;
const getVerificationMethod = (did, key, resolver) => __awaiter(void 0, void 0, void 0, function* () {
    var _a;
    const didDocument = yield (0, exports.resolve)(did, resolver);
    if (!didDocument)
        return;
    return (_a = didDocument.verificationMethod) === null || _a === void 0 ? void 0 : _a.find((k) => k.id.toLowerCase() === key.toLowerCase());
});
exports.getVerificationMethod = getVerificationMethod;
