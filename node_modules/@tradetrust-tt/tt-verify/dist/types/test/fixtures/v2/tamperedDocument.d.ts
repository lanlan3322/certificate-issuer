import { v2, WrappedDocument } from "@tradetrust-tt/tradetrust";
interface CustomDocument extends v2.OpenAttestationDocument {
    name: string;
    issuedOn: string;
    transcript: any;
    issuers: {
        url: string;
        name: string;
        certificateStore: string;
    }[];
    recipient: {
        name: string;
        email: string;
        phone: string;
    };
}
export declare const tamperedDocument: WrappedDocument<CustomDocument>;
export declare const tamperedDocumentInvalid: WrappedDocument<CustomDocument>;
export {};
