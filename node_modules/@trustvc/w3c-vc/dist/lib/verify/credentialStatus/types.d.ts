import { BitstringStatusListCredentialStatus, CredentialStatusPurpose } from '@trustvc/w3c-credential-status';
import { CredentialStatus as CredentialStatus$1 } from '../../types.js';
import '@mattrglobal/jsonld-signatures-bbs';
import '@digitalbazaar/data-integrity';

type CredentialStatus = CredentialStatus$1 | BitstringStatusListCredentialStatus;
type CredentialStatusResult = {
    status?: boolean;
    purpose?: CredentialStatusPurpose;
    error?: string;
};

export type { CredentialStatus, CredentialStatusResult };
