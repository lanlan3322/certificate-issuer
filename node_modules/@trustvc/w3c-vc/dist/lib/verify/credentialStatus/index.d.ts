import { CredentialStatusType } from '@trustvc/w3c-credential-status';
import { CredentialStatus, CredentialStatusResult } from './types.js';
import { DocumentLoader } from '@trustvc/w3c-context';
import '../../types.js';
import '@mattrglobal/jsonld-signatures-bbs';
import '@digitalbazaar/data-integrity';

declare const verifyCredentialStatus: (credentialStatus: CredentialStatus, type?: CredentialStatusType, options?: {
    documentLoader?: DocumentLoader;
}) => Promise<CredentialStatusResult>;

export { verifyCredentialStatus };
