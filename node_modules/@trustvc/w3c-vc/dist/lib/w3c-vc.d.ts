import { DocumentLoader } from '@trustvc/w3c-context';
import { PrivateKeyPair } from '@trustvc/w3c-issuer';
import { RawVerifiableCredential, SignedVerifiableCredential, CryptoSuiteName, SigningResult, VerificationResult, DerivedResult } from './types.js';
import '@mattrglobal/jsonld-signatures-bbs';
import '@digitalbazaar/data-integrity';

declare const isRawDocument: (document: RawVerifiableCredential | unknown) => boolean;
declare const isRawDocumentV1_1: (document: RawVerifiableCredential | unknown) => boolean;
declare const isRawDocumentV2_0: (document: RawVerifiableCredential | unknown) => boolean;
declare const isSignedDocument: (document: SignedVerifiableCredential | unknown) => document is SignedVerifiableCredential;
declare const isSignedDocumentV1_1: (document: SignedVerifiableCredential | unknown) => document is SignedVerifiableCredential;
declare const isSignedDocumentV2_0: (document: SignedVerifiableCredential | unknown) => document is SignedVerifiableCredential;
declare const isDerived: (document: SignedVerifiableCredential) => Promise<boolean>;
declare const signCredential: (credential: RawVerifiableCredential, keyPair: PrivateKeyPair, cryptoSuite?: CryptoSuiteName, options?: {
    documentLoader?: DocumentLoader;
    mandatoryPointers?: string[];
}) => Promise<SigningResult>;
declare const verifyCredential: (credential: SignedVerifiableCredential, options?: {
    documentLoader?: DocumentLoader;
}) => Promise<VerificationResult>;
declare const deriveCredential: (credential: SignedVerifiableCredential, revealedAttributes: string[], options?: {
    documentLoader?: DocumentLoader;
}) => Promise<DerivedResult>;

export { deriveCredential, isDerived, isRawDocument, isRawDocumentV1_1, isRawDocumentV2_0, isSignedDocument, isSignedDocumentV1_1, isSignedDocumentV2_0, signCredential, verifyCredential };
