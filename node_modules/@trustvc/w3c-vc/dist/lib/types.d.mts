import { BbsBlsSignature2020, BbsBlsSignatureProof2020 } from '@mattrglobal/jsonld-signatures-bbs';
import { DataIntegrityProof } from '@digitalbazaar/data-integrity';

interface SigningResult {
    signed?: SignedVerifiableCredential;
    error?: string;
}
interface DerivedResult {
    derived?: SignedVerifiableCredential;
    error?: string;
}
interface VerificationResult {
    verified: boolean;
    error?: string;
}
type CredentialStatus = {
    id?: string;
    type: string;
} & Record<string, any>;
type CredentialStatuses = CredentialStatus | CredentialStatus[];
type CredentialSchema = {
    id: string;
    type: string;
} & Record<string, any>;
type CredentialSchemas = CredentialSchema | CredentialSchema[];
type CredentialSubject = Record<string, any>;
type CredentialSubjects = CredentialSubject | CredentialSubject[];
type TermsOfUse = {
    type: string;
} & Record<string, any>;
type TermsOfUses = TermsOfUse | TermsOfUse[];
type RelatedResource = {
    id: string;
    mediaType?: string;
} & ({
    digestSRI: string;
    digestMultibase?: string;
} | {
    digestSRI?: string;
    digestMultibase: string;
}) & Record<string, any>;
type RelatedResources = RelatedResource | RelatedResource[];
type RefreshService = {
    type: string;
} & Record<string, any>;
type RefreshServices = RefreshService | RefreshService[];
type Evidence = {
    type: string;
} & Record<string, any>;
type Evidences = Evidence | Evidence[];
type Proof = {
    type: string;
    created?: string;
    proofPurpose: string;
    verificationMethod: string;
    proofValue: string;
    nonce?: string;
} & Record<string, any>;
type VerifiableCredential = SignedVerifiableCredential | RawVerifiableCredential;
type SignedVerifiableCredential = {
    '@context': string | string[];
    id: string;
    type: string | string[];
    issuer: string | Record<string, any>;
    issuanceDate?: string;
    validFrom?: string;
    validUntil?: string;
    expirationDate?: string;
    credentialStatus?: CredentialStatuses;
    credentialSubject: CredentialSubjects;
    credentialSchema?: CredentialSchemas;
    termsOfUse?: TermsOfUses;
    evidence?: Evidences;
    relatedResource?: RelatedResources;
    refreshService?: RefreshServices;
    renderMethod?: Record<string, any>;
    qrCode?: Record<string, any>;
    proof?: Proof;
} & Record<string, any>;
type RawVerifiableCredential = Omit<SignedVerifiableCredential, 'proof'>;
type CryptoSuiteName = 'BbsBlsSignature2020' | 'bbs-2023' | 'ecdsa-sd-2023';
type ProofType = 'BbsBlsSignature2020' | 'BbsBlsSignatureProof2020' | 'DataIntegrityProof';
declare const proofTypeMapping: Record<ProofType, typeof BbsBlsSignature2020 | typeof BbsBlsSignatureProof2020 | typeof DataIntegrityProof>;

export { type CredentialSchema, type CredentialSchemas, type CredentialStatus, type CredentialStatuses, type CredentialSubject, type CredentialSubjects, type CryptoSuiteName, type DerivedResult, type Evidence, type Evidences, type Proof, type ProofType, type RawVerifiableCredential, type RefreshService, type RefreshServices, type RelatedResources, type SignedVerifiableCredential, type SigningResult, type TermsOfUse, type TermsOfUses, type VerifiableCredential, type VerificationResult, proofTypeMapping };
