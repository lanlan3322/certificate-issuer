import { GasStationFeeData } from '../gasStation/index.js';
import { networkName, networkType, networkCurrency } from '../network/index.js';
import 'ethers';

declare enum CHAIN_ID {
    local = "1337",
    mainnet = "1",
    matic = "137",
    amoy = "80002",
    sepolia = "11155111",
    xdc = "50",
    xdcapothem = "51",
    stabilitytestnet = "20180427",
    stability = "101010",
    astron = "1338",
    astrontestnet = "21002"
}
type chainInfo = {
    id: CHAIN_ID;
    label: string;
    name: networkName;
    type: networkType;
    currency: networkCurrency;
    iconImage: string;
    explorerUrl: string;
    explorerApiUrl?: string;
    rpcUrl?: string;
    gasStation?: () => Promise<GasStationFeeData | undefined>;
    nativeCurrency?: {
        name: string;
        symbol: string;
        decimals: number;
    };
};
type supportedChains = Record<CHAIN_ID, chainInfo>;
declare const SUPPORTED_CHAINS: supportedChains;

export { CHAIN_ID, SUPPORTED_CHAINS, type chainInfo };
