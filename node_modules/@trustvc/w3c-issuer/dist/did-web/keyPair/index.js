'use strict';

var crypto = require('crypto');
var lib = require('../../lib');
var types = require('../../lib/types');
var bls12381 = require('./bls12381');
var bbs2023 = require('./bbs2023');
var ecdsaSd2023 = require('./ecdsaSd2023');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var crypto__default = /*#__PURE__*/_interopDefault(crypto);

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
const generateModernCryptosuite = /* @__PURE__ */ __name(async (type, keyPairOptions) => {
  switch (type) {
    case types.CryptoSuite.Bbs2023:
      return await bbs2023.generateBbs2023KeyPair(keyPairOptions);
    case types.CryptoSuite.EcdsaSd2023:
      return await ecdsaSd2023.generateEcdsaSd2023KeyPair();
    default:
      throw new Error(`Unsupported modern cryptosuite: ${type}`);
  }
}, "generateModernCryptosuite");
const generateKeyPair = /* @__PURE__ */ __name(async (keyPairOptions) => {
  const { type, seedBase58, privateKeyBase58, publicKeyBase58 } = keyPairOptions;
  if (!type) {
    throw new Error("Invalid key pair type");
  }
  let seed;
  const seedSupportedTypes = [
    types.VerificationType.Bls12381G2Key2020,
    types.CryptoSuite.Bbs2023
  ];
  if (seedSupportedTypes.includes(type)) {
    if (seedBase58) {
      seed = await lib.parseMultibase("z" + seedBase58);
    }
    keyPairOptions.seed = seed ?? Uint8Array.from(crypto__default.default.randomBytes(32));
  }
  let generatedKeyPair;
  if (Object.values(types.CryptoSuite).includes(type)) {
    generatedKeyPair = await generateModernCryptosuite(type, keyPairOptions);
  } else {
    switch (type) {
      case types.VerificationType.Ed25519VerificationKey2018:
        throw new Error("Unsupported key pair type");
      case types.VerificationType.Bls12381G2Key2020:
        generatedKeyPair = await bls12381.generateBls12381KeyPair(keyPairOptions);
        break;
      default:
        throw new Error("Unsupported key pair type");
    }
  }
  if (seedBase58 && type === types.VerificationType.Bls12381G2Key2020) {
    generatedKeyPair = generatedKeyPair;
    if (privateKeyBase58 && privateKeyBase58 !== generatedKeyPair.privateKeyBase58) {
      throw new Error("Private key does not match");
    }
    if (publicKeyBase58 && publicKeyBase58 !== generatedKeyPair.publicKeyBase58) {
      throw new Error("Public key does not match");
    }
  }
  return generatedKeyPair;
}, "generateKeyPair");

exports.generateKeyPair = generateKeyPair;
