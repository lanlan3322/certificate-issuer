name: New MCP server
version: 0.0.1
schema: v1

mcpServers:
  - name: filesystem
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - /Volumes/eSYSTEM-D/certificate-issuer
