# DID and did:web

Use `did:web` to publish an issuer DID document from a controlled web domain. DID documents identify verification methods, while signing keys remain in a protected server-side signing service.