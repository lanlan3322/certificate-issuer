# Verification

Verification checks required VC fields and proof validity. Wallet proofs recover the signing address; DID proofs are validated with TrustVC. Ethereum credentials also require document-store status verification.